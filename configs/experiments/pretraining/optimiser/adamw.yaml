defaults:
  - scheduler: warmup_cosine

optimiser:
  _target_: torch.optim.AdamW
  lr: 0.0004
  weight_decay: 0.1
  params: ???
  _convert_: 'all'

#  warmup steps as percentage of
#  the total steps
warmup_perc: 0.01
